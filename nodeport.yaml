apiVersion: v1
kind: Service
metadata:
  name: webpage-nodeport
  namespace: default
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: webpage
  externalIPs: 
    - 54.144.9.249
